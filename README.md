## Generate Lambda function Zip for AWS Lambda

#### Execute
``` aws lambda update-function-code --function-name generate_pre_signed_url --zip-file fileb://function.zip --profile profilename --region eu-west-1```

